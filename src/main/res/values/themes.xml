<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AcademicPlanner" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AcademicPlanner" parent="Base.Theme.AcademicPlanner">
        <item name="toolbarStyle">@style/Widget.MyApp.Toolbar</item>
    </style>

    <style name="Widget.MyApp.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/CustomToolbarTextAppearance</item>
    </style>

    <style name="Theme.AcademicPlanner.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AcademicPlanner.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AcademicPlanner.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomToolbarTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:fontFamily">@font/dm_sans_bold</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="SpinnerFont" parent="Widget.AppCompat.Spinner">
        <item name="android:fontFamily">@font/dm_sans</item>
    </style>


</resources>

